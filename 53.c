// checking of no. divisible by 2 and 3 both
#include<stdio.h>
int main()
{
    int n;
    printf("enter the value of n");
    scanf("%d",&n);
    if(n%6==0)
    printf("divisible");
    else
    printf("non divisible");
    return 0;
}
