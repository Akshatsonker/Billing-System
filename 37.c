// adding digit in a no.
#include<stdio.h>
int main()
{
    int x,y,z;
    printf("enter the value of x,y");
    scanf("%d %d",&x,&y);
    z=x*10+y;
    printf("the value of is %d",z);
    return 0;
}