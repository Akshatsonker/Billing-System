// print greater no.
#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the value of a,b");
    scanf("%d %d",&a,&b);
    if(a>b)
    {
        printf("greater no. a is %d",a);
    }
    else
    {
    printf("greater no. b  is %d",b);
    }
    return 0;
}