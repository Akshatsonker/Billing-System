//print string
#include<stdio.h>
#include<string.h>
int main()
{
    char str[20]={'a','k','s','h','a','t'};
   // int i;
   // printf("print the name");
   // for(i=0 ; str[i] ; i++)
   // scanf("%c",str[i]);
    printf("%s",str);
    return 0;
}